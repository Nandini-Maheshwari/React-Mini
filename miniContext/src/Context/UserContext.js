import React from 'react'

const UserContext = React.createContext()
// It wraps up everthing
// It's like a global variable n all components wrapped inside it have access to it

export default UserContext;


// all we've done in this file is create a context